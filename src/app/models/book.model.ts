export class Book {
    bookId: number;
    book: string;
    author: string;
    constructor(bookId: number, book: string, author: string) {
      this.bookId = bookId;
      this.book = book;
      this.author = author;
    }
  }
  